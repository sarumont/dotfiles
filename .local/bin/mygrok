#!/bin/sh

#DOMAIN=".sigil.org"
#REMOTE="$2$DOMAIN"
REMOTE_SSH_PORT="22"
if test $# -gt 1; then
	REMOTE_SSH_PORT=$2
fi
REMOTE_SSH_ARGS="-i ~/.ssh/id_rsa_ngrok"
ssh -tR 1081:127.0.0.1:$1 mygrok "ssh $REMOTE_SSH_ARGS -Nl \$USER -L 0.0.0.0:$1:127.0.0.1:1081 localhost -p$REMOTE_SSH_PORT"
