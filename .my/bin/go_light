#!/bin/sh

set +e
mkdir -p ~/.config/local

echo "--theme=OneHalfLight" > ~/.config/bat/config
echo "light" > ~/.config/local/theme

case $(uname) in
    Linux)
        command cat $HOME/.config/termite/config.base $HOME/.config/termite/config.light > $HOME/.config/termite/config
        killall -USR1 termite
        echo 'gtk-icon-theme-name = "Flat-Remix-Blue-Light"' > ~/.gtkrc-2.0
        echo 'gtk-theme-name = "Arc"' >> ~/.gtkrc-2.0
        gsettings set org.gnome.desktop.interface gtk-theme "Arc"
        gsettings set org.gnome.desktop.interface icon-theme "Flat-Remix-Blue-Light"
        ;;
    Darwin)
        ;;
    *)
        echo "Really? $(uname)?"
        ;;
esac
